.idea
node_modules
artifacts
cache
.env
**/*.DS_Store
.log/

# generated by nix-pre-commit-hooks
/.pre-commit-config.yaml

# direnv cache
.direnv/

# prevent our formatters from formatting code in submodule
/arbitrum

# rust
Cargo.lock
target/

# python / brownie
build/
__pycache__/
*.py[cod]

# ./doc/contracts is the documentation repo.
# we want autogenerated stuff to go there as well, so we are gitignore'ing it.
# static files should still be force added to that directory though
/doc/contracts

# typescript contract bindings
/typechain/
