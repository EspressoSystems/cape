[package]
name = "cape-workflow"
version = "0.1.0"
edition = "2018"

[dependencies]
jf-aap = { features=["std"], git = "ssh://git@github.com/SpectrumXYZ/jellyfish-apps.git", branch = "plonk-verifier" }
jf-utils = { features=["std"], git = "ssh://git@github.com/SpectrumXYZ/jellyfish-apps.git", branch = "plonk-verifier" }
jf-rescue = { features=["std"], git = "ssh://git@github.com/SpectrumXYZ/jellyfish-apps.git", branch = "plonk-verifier" }
ethers = { git = "https://github.com/gakonst/ethers-rs", features = ["abigen"] }

serde_json = "1.0.67"
serde = { version = "1.0.124", features = ["derive"] }
rand = "0.8.4"
itertools = "0.10.1" # see Nathan M's comment: https://gitlab.com/translucence/cap-on-ethereum/cape/-/merge_requests/57#note_740626024
